#include <stdio.h>
// #include <cmath>./>":"

// c i c++ można w zadaniach pisać
// wyklady i co na tablicy bedzie c
// i powinno sie to samo napisac w c jak ktos umie to w cpp napisac
// bedzie sie pytac jak printfem zrobic jak ktos cout uzywa
// c w 95% na kolokwiach
// na pierwsze wejsciowki c bedzie tylko mozliwe a potem juz na pozniejszych mozna cpp pisac
// trzeba sie zapytac jak dojdzie do wejsciowki czy mozna pisac w cpp program
// using namespace std;

// FUNCKJE

// 1. Napisz program, który wczytuje ze standardowego wejścia liczbę całkowitą n i wypisuje na standardowe
// wyjście wartość bezwzględną z n. Do rozwiązania zadania nie używaj funkcji bibliotecznych za wyjątkiem
// operacji wejścia/wyjścia. W programie użyj samodzielnie zaimplementowanej funkcji liczącej wartość
// bezwzględną.
// int num;
// int * result = &num;
// int * absolute_value() {
//     cin >> num;
//     if (num < 0) {
//         * result = num * -1;
//         return result;
//     }
//     return result;
// // }
// void zad1(){
//     cout << *absolute_value() << endl;
// }
// int zad2(){
//     int number;
//     int result = 1;
//     do{
//         // Give user instruction
//         cout << "Please enter a poisitive number" << endl;
//         cin >> number;
//         if (number >=0){
//             for (int i=1; i<=number;i++){
//             result*=i;

//             }
//         }
//     }
//     while(cin>>number);
//     return result;

// }

// 2. Napisz program, który wczytuje ze standardowego wejścia nieujemną liczbę całkowitą n i wypisuje na
// standardowym wyjściu liczbę n!. W programie użyj samodzielnie zaimplementowanej funkcji liczącej
// wartość silni.
// void zad2_s(){
//     silnia();
// }
// 3. Napisz program, który wczytuje ze standardowego wejścia nieujemną liczbę całkowitą n (n > 2) i wypisuje
// na standardowym wyjściu największą liczbę k taką, że k dzieli n i k < n. Algorytm wyszukiwania liczby k
// spełniającej powyższe warunki umieść w oddzielnej funkcji.
void zad3()
{
}
// 4. Napisz funkcję, która dostaje jako argument nieujemną liczbę całkowitą n i zwraca jako wartość liczbę 2n
// .
void zad4()
{
}
// 5. Napisz funkcję, która dostaje jako argument liczbę całkowitą n i zwraca jako wartość liczbę 2n
// .
void zad5()
{
}
// 6. Napisz funkcję, która dostaje jako argumenty nieujemne liczby całkowite n i m, z których co najmniej jedna
// jest rożna od zera i zwraca jako wartość nm.
void zad6()
{
}
// 7. Napisz funkcję, która dostaje jako argumenty liczby całkowite n i m, z których co najmniej jedna jest rożna
// od zera i zwraca jako wartość nm.
void zad7()
{
}
// 8. Napisz funkcję, która dostaje jako argumenty liczbę dodatnią n i zwraca jako wartość ⌊√n⌋. Rozwiąż zadanie
// nie wykorzystując funkcji bibliotecznych.
void zad8()
{
}
// 9. Napisz funkcję, która dostaje jako argumenty liczbę całkowitą m (m > 1) oraz nieujemną liczbę n i zwraca
// jako wartość ⌊ Ȃ
// 
// ⌋. Rozwiąż zadanie nie wykorzystując funkcji bibliotecznych.
void zad9()
{
}

// 10. Napisz program, który wczytuje ze standardowego wejścia nieujemną liczbę całkowitą n i wypisuje na
// standardowym wyjściu sumę liczb mniejszych od n i zarazem względnie pierwszych z n. Algorytm
// wyliczania sumy podziel na dwie funkcje.
void zad10()
{
}
// 11. Napisz program, który wczytuje ze standardowego wejścia nieujemną liczbę całkowitą n i wypisuje na
// standardowym wyjściu następującą sumę _√0_ + _√1_ + . . . + ⌊√n⌋. Algorytm wyliczania sumy podziel na
// dwie funkcje.
void zad11()
{
}
// 12. Napisz program, który wczytuje ze standardowego wejścia nieujemne liczby całkowitą n i m (m > 1), i
// wypisuje na standardowym wyjściu następującą sumę _ m√0_ + _ m√1_ + . . . + ⌊ m√n⌋. Algorytm wyliczania
// sumy podziel na dwie funkcje.
void zad12()
{
}

// 13. Napisz funkcję, która dostaje jako argument dodatnią liczbę całkowitą n i wypisuje na standardowym wyjściu
// wszystkie możliwe rozkłady liczby n na sumy dwóch kwadratów dodatnich liczb całkowitych. Rozważ dwa
// przypadki (jeżeli zajdzie taka potrzeba, możesz w rozwiązaniu używać funkcji pomocniczych):
// a. gdy „a2 + b2
// ” i „b2 + a2
// ” dla a 6= b traktujemy jako dwa równe rozkłady,
// b. gdy „a2 + b2
// ” i „b2 + a2
// ” traktujemy jako ten sam rozkład i wypisujemy tylko jedne z nich.
void zad13()
{
}

// 14. Napisz funkcję, która dostaje jako argument dodatnią liczbę całkowitą n i wypisuje na standardowym wyjściu
// wszystkie możliwe rozkłady liczby n na sumy kwadratów dodatnich liczb całkowitych. Rozważ dwa
// przypadki analogiczne do tych z zadania 2.2.13. Jeżeli zajdzie taka potrzeba możesz w rozwiązaniu używać
// funkcji pomocniczych.
void zad14()
{
}
// 15. Napisz funkcję, która dostaje jako argumenty dodatnie liczby całkowite n i m, i wypisuje na standardowym
// wyjściu wszystkie możliwe rozkłady liczby n na sumę dwóch dodatnich liczb całkowitych podniesionych do
// potęgi m. Rozważ dwa przypadki analogiczne do tych z zadania 2.2.13. Jeżeli zajdzie taka potrzeba możesz
// w rozwiązaniu używać funkcji pomocniczych.
void zad15()
{
}

// 16. Napisz funkcję, która dostaje jako argumenty dodatnie liczby całkowite n i m, i wypisuje na standardowym
// wyjściu wszystkie możliwe rozkłady liczby n na sumę dodatnich liczb całkowitych podniesionych do potęgi
// m. Rozważ dwa przypadki analogiczne do tych z zadania 2.2.13. Jeżeli zajdzie taka potrzeba możesz w
// rozwiązaniu używać funkcji pomocniczych.
void zad16()
{
}
// 17. Napisz funkcję, która zlicza i wypisuje na standardowym wyjściu liczbę swoich wywołań.
void zad17()
{
}
// 18. Napisz funkcję generującą liczby pseudolosowe. Pierwszą wartością funkcji powinna być dowolna liczba z
// przedziału (0, 1). Kolejne wartości powinny być wyliczane ze wzoru  = 1 − 
// 
// , gdzie xn to aktualna,
// a xn−1 to poprzednia wartość funkcji.
void zad18()
{
}

// 19. Napisz funkcję, która wczytuje ze standardowego wejścia liczbę całkowitą i zwraca ją jako swoją wartość.
// Dodatkowo funkcja powinna sumować wszystkie dotychczas wczytane wartości i przy każdym swoim
// wywołaniu wypisywać na standardowym wyjściu ich aktualną sumę .
void zad19()
{
}

// 20. Napisz rekurencyjną funkcję, która dla otrzymanej w argumencie nieujemnej całkowitej liczby n zwraca jako
// wartość n!.

//  if number<2 {
//      return 1;
// }
// else{
//      return number*zad20(number - 1);
// }
unsigned long long int zad20(unsigned int number){return (number<0)? "Error! number < 0 " :(number<2 && number>0) ?1  :(1*number * zad20(number - 1));}
// this. is the same as the code below:

// int zad20(int number)
// {
//     if (number < 2 && number > -1)
//     {
//         return 1;
//     }
//     else if (number < 0)
//     {
//         printf("Cannot be less than 0");
//         return 0;
//     }
//     else
//     {
//         return number * zad20(number - 1);
//     }
// }

// int zadw(int num)
// {
//     if (num < 2 && num > 0)
//     {
//         return 1;
//     }
//     else if (num < 0)
//     {
//         printf("Error! The number cannot be smaller than 0.");
//         return 0;
//     }
//     else
//     {
//         return num * (zadw(num - 1));
//     }
// }

// int zad2_ss(){
//     int number;
//     int result = 1;
//     do{
//         // Give user instruction
//         cout << "Please enter a poisitive number" << endl;
//         cin >> number;
//         if (number >=0){
//             for (int i=1; i<=number;i++){
//             result*=i;

//             }
//         }
//     }
//     while(cin>>number);
//     return result;

// }
// 21. Napisz rekurencyjną funkcję zwracającą dla otrzymanej w argumencie nieujemnej liczby całkowitej n
// wartość elementu o indeksie n ciągu zdefiniowanego w następujący sposób:
// a0 = 1, an = 2 ∗ an−1 + 5 dla n > 0.
int zad21(int num)
{
    if (num = 0)
    {
        return 1;
    }
    else
    {
        int result;
        int a = 1;
        for (int i = 1; i <= num; i++)
        {
            result = 2 * a + 5;
            a = result;
        }
        return result;
    }
}

// 22. Napisz rekurencyjną funkcję zwracającą dla otrzymanej w argumencie nieujemnej liczby całkowitej n
// wartość elementu o indeksie n ciągu zdefiniowanego w następujący sposób:
// a0 = a1 = 1, an = an−1 + 2 ∗ an−2 + 3 dla n > 1
void zad22()
{
}

// 23. Napisz rekurencyjną funkcję zwracającą dla otrzymanej w argumencie nieujemnej liczby całkowitej n
// wartość elementu ciągu Fibonacciego o indeksie n.
void zad23()
{
}

// 24. Napisz rekurencyjną funkcję zwracającą dla otrzymanej w argumencie nieujemnej liczby całkowitej n
// wartość elementu o indeksie n ciągu zdefiniowanego w następujący sposób:
// a0 = a1 = 1, an = a0 + a1 + ... + an−1 dla n > 1
void zad24()
{
}

// 25. Napisz funkcję rekurencyjną, która dla otrzymanej w argumencie nieujemnej liczby całkowitej n zwraca
// wartość elementu o indeksie n ciągu zdefiniowanego w następujący sposób
// a0 = a1 = 1, an = an−1 + n dla n parzystych, an = an−1 ∗ n dla n nieparzystych.
void zad25()
{
}

// 26. Napisz funkcję rekurencyjną, która dla otrzymanej w argumencie nieujemnej liczby całkowitej n zwraca
// wartość elementu o indeksie n ciągu zdefiniowanego w następujący sposób:
// a0 = a1 = a2 = 1 oraz dla k > 2, a3·k = a3·k−1 + a3·k−2, a3·k+1 = 5 ∗ a3·k + 4, a3·k+2 = a3·k+1.
void zad26()
{
}

// 27. Napisz funkcję rekurencyjną, która dla otrzymanej w argumentach pary nieujemnych liczb całkowitych n i
// m zwraca wartość f(n,m) gdzie funkcja f jest zdefiniowana w następujący sposób:
// f(n, 0) = n, f(0,m) = m, f(n,m) = f(n − 1,m) + f(n,m− 1) + f(n − 1,m − 1) dla n,m > 0.
void zad27()
{
}

// 28. Napisz funkcję rekurencyjną, która dla otrzymanej w argumentach pary nieujemnych liczb całkowitych n i
// m zwraca wartość f(n,m) gdzie funkcja f jest zdefiniowana w następujący sposób:
// f(n, 0) = n
// f(n,m) = f(m, n)
// f(n,m) = n − m + f(n − 1,m) + f(n,m− 1) dla n ≥ m > 0.
void zad28()
{
}

// 29. Napisz rekurencyjną funkcję, która dostaje jako argumenty dwie dodatnie liczby całkowite n i m, i zwraca
// jako wartość największy wspólny dzielnik tych liczb obliczony algorytmem Euklidesa.
void zad29()
{
}

int main()
{
    // zad1();

    // zad2();

    // zad3();

    // zad4();

    // zad5();

    // zad6();

    // zad7();

    // zad8();

    // zad9();

    // zad10();

    // zad11();

    // zad12();

    // zad13();

    // zad14();

    // zad15();

    // zad16();

    // zad17();

    // zad18();

    // zad19();

    printf("%d\n", zad20(6));

    // printf("%d",zad21(2));
    // zadw(6);
    // zad22();

    // zad23();

    // zad24();

    // zad25();

    // zad26();

    // zad27();

    // zad28();

    // zad29();
}
