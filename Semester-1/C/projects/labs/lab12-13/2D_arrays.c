// STRUKTURY
// 1. Zdefiniuj strukturę trojkat przechowującą długości boków trójkąta. Napisz funkcję, która otrzymuje jako
// argument zmienną typu struct trojkat, i zwraca jako wartość obwód trójkąta przekazanego w argumencie.
// 2. Napisz funkcję, która otrzymuje jako argumenty zmienną troj1 typu struct trojkat zdefiniowanego w
// poprzednim zadaniu oraz zmienną troj2 wskaźnik na zmienną typu struct trojkat, i przepisuje zawartość
// zmiennej troj1 do zmiennej wskazywanej przez troj2.
// 3. Zdefiniuj strukturę punkt służącą do przechowywania współrzędnych punktów w trójwymiarowej
// przestrzeni kartezjańskiej. Napisz funkcję, która otrzymuje jako argumenty tablicę tab o argumentach typu
// struct punkt oraz jej rozmiar, i zwraca jako wartość najmniejszą spośród odległości pomiędzy punktami
// przechowywanymi w tablicy tab. Zakładamy, że otrzymana w argumencie tablica ma co najmniej dwa
// argumenty.
// 4. Napisz funkcję, która otrzymuje jako argumenty tablice tab1 i tab2 o argumentach typu struct punkt
// zdefiniowanego w rozwiązaniu poprzedniego zadania oraz ich rozmiar, i przepisuje zwartość tablicy tab1 do
// tablicy tab2.
// 5. Zdefiniuj strukturę punktn służącą do przechowywania współrzędnych punktów w n-wymiarowej
// przestrzeni kartezjańskiej. Do przechowywania poszczególnych wymiarów wykorzystaj tablicę nelementową. W strukturze punktn przechowuj także ilość wymiarów przestrzeni. Napisz funkcję, która
// otrzymuje jako argumenty tablice tab1 i tab2 o argumentach typu struct punktn oraz ich wspólny rozmiar, i
// przepisuje zwartość tablicy tab1 do tablicy tab2. Zakładamy, że tablica tab2 jest pusta (czyli nie musimy się
// martwić o jej poprzednią zawartość).
// 6. Zdefiniuj strukturę zespolone służącą do przechowywania liczb zespolonych. Zdefiniowana struktura
// powinna zawierać pola im i re typu double służące do przechowywania odpowiednio części urojonej i
// rzeczywistej liczby zespolonej. Napisz funkcję dodaj, która dostaje dwa argumenty typu zespolone i zwraca
// jako wartość ich sumę.
// 7. Zdefiniuj strukturę student służącą do przechowywania danych osobowych studenta (struktura powinna
// zawierać takie pola, jak: imie, nazwisko, adres, pesel, kierunek i numer legitymacji). Napisz funkcję, która
// otrzymuje jako argument wskaźnik na strukturę student i wczytuje dane ze standardowego wejścia do
// rekordu wskazywanego przez argument funkcji.